
# 服务端口
port: 6100

grpc: 6101

debug: true # gin debug模式

# 数据库设置
db:
  serverType: postgres # 数据库服务器类型当前模板可选(postgres postgresql)
  username: postgres # 用户名
  password: root # 密码
  host: localhost # 服务器host
  port: 5432 # 服务端口
  database: ai_lab # db名称
  maxOpenConns: 10 # 数据库连接池最大数量
  maxIdleConns: 2 # 数据库空闲连接池最大数量
  debug:        true #数据库debug模式

rabbitmq:
  user:     admin # 用户名
  password: apulis123 # 密码
  host:     127.0.0.1 # host地址
  port:     0 # mq端口 5672

resources:
  model:          http://workshop.bmod:6110                #模型管理接口
  dataset:        http://adhub.bmod:6120                   #数据集管理接口
  jobsched:       http://job-sched.bmod:6130/api/v1/jobs   #job调度管理接口
  engine:         http://adharbor.bmod:6140                #镜像管理接口
  code:                                                    #代码管理暂时由模型工厂托管

mounts:
  ai-lab-data:   /app

